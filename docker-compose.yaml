version: '3.8'
services:
  db:
    image: postgres:15.3
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=12345678
      - POSTGRES_DB=xcs
    restart: always
    container_name: db
    ports:
      - '5432:5432'
