# Examples

The files in this directory are deliberately non-live examples. Placeholder addresses and schema UIDs must be replaced before validation or transaction construction.

XCS v0.1 permits only one simultaneous Credential for a given issuer, subject and schema UID. The course example therefore represents one specific program. An issuer that needs several simultaneous course completions for the same subject must use distinct schema UIDs in v0.1.
